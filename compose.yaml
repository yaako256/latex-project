# compose.yaml

name: LatexProgect

services:
  latex:
    # コンテナ名
    container_name: ${CONTAINER_NAME}

    # Dockerfileを使ってbuildする
    build:
      context: .
      dockerfile: ./Dockerfile
    
    volumes:
      - .:/work
      # ビルド補助スクリプトをルートにマウント
      #- ./scripts/build_latex.sh:/work/build_latex.sh
    working_dir: /work
    
    command: >
      bash
    tty: true