{
  "latex-workshop.latex.tools": [
    {
      "name": "docker",
      // コンテナ外から叩くため、変更する
      "command": "docker",
      "args": [
        // コンテナの中に入る
        "compose", "exec", "latex",
        // latex command
        "latexmk",
        // .texがあるディレクトリを作業ディレクトリにする
        "-cd",
        "-auxdir=trash",
        "-outdir=.",
        "-synctex=1",
        "-interaction=nonstopmode",
        "-file-line-error",
        // .texがあるディレクトリを相対パスで指定
        // Dockerを使っているので絶対パスはよろしくない。
        "%RELATIVE_DOC%"
      ]
    }
  ],
  "latex-workshop.latex.recipes": [
    {
      "name": "docker",
      "tools": ["docker"]
    }
  ]
}